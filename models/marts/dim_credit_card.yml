models:
  - name: dim_credit_card
    description: dimensão de cartão de credito do sistema pdv da northwind.
    columns:
      - name: credit_card_id
        description: para preencher.
        data_tests: ['unique', 'not_null']

      - name: card_type
        description: para preencher.
